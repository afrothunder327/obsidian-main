
##### <span style="color:orange">On this day</span>
```dataviewjs
// set your own minimum year and the path to your journals (if applicable)
const minYear = 2024;
const journalPath = 'Journals/Daily';

const rangeOfYears = (start, end) => Array(end - start + 1)
  .fill(start)
  .map((year, index) => year + index);
const d = moment(page.file.name, "YYYY-MM-DD");
const currentYear = d.year();
const availableYears = rangeOfYears(minYear, currentYear);
const month = ("0" + (d.month() + 1)).slice(-2);
const day = ("0" + (d.date())).slice(-2);
const dateString = month + '-' + day;

availableYears.forEach((y) => {
	var note = dv.page(`${journalPath}/${y}-${dateString}`);
	if (note) {
		dv.el('span', `[[${note.file.path}|${y}]] `);
	}
});
```
---
## Journal Entry


## Tasks
> [!col] 
>> [!col-md]
>> ### Overdue
>> ```tasks
>> not done
>> due before 2023-01-11
>> hide backlink
>> hide edit button
>> ```
>
>>[!col-md]
>> ### Today
>> ```tasks
>> happens on 2023-01-11
>> sort by status.type
>> sort by priority
>> sort by due
>> hide backlink
>> hide edit button
>> ```
>
>>[!col-md]
>>### Upcoming
>>```tasks
>>happens in 2023-01-11 2023-01-25
>>hide edit button
>>hide backlink
>>```
## Notes from today
```dataview
LIST
FROM !"Journals"
WHERE file.cday = date("2023-01-11")
```
