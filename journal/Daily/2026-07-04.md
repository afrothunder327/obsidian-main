---
type: daily
tags: [daily]
---
##### On this day
```dataviewjs
const minYear = 2024;
const journalPath = 'Journal/Daily';
const rangeOfYears = (start, end) => Array(end - start + 1).fill(start).map((y, i) => y + i);
const d = moment(dv.current().file.name, "YYYY-MM-DD");
const currentYear = d.year();
const availableYears = rangeOfYears(minYear, currentYear - 1);
const month = ("0" + (d.month() + 1)).slice(-2);
const day = ("0" + (d.date())).slice(-2);
const dateString = month + '-' + day;
availableYears.forEach((y) => {
	var note = dv.page(`${journalPath}/${y}-${dateString}`);
	if (note) { dv.el('span', `[[${note.file.path}|${y}]] `); }
});
```

## Plan


## Notes
- Watching this documentary about the American revolution. The quelling of the Newburgh conspiracy seems like a real hinge of history moment.
- It tells the story of George Washington as extremely humble and inherently democratic. 
- Where can I take better notes on this series? 
- Founding Fathers were deeply fearful of demagogues.
- Popular election of senators on started in 1913. Before that the house of representatives picked them I think. I learned this before - there was some corruption thing that made this the case. 
- Wow the constitution is just riven with stuff about slavery.
- What did I learn from the fifth episode?
	- You must take notes on this stuff.
	- George Washington and French Indian war
	- Stamp act and tea act
	- Military setbacks
	- Washington vs taxation tradeoff
	- 

## End of day
- Shipped:
- Open threads:
- Tomorrow:

## Notes created today
```dataview
LIST
FROM !"Journal"
WHERE file.cday = date("2026-07-04")
```
